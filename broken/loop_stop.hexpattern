#git(/libs/base.hexpattern)

/*
Stops a spell in a cassette called "Loop" started via "loop start".
Leaves the cassette locked as "Loop" to prevent kicking when queueing diffrent spell.
*/

{
    {@str(Loop)} >>
    Program Purification
    Numerical Reflection: 200
    {@str(Loop)} >>
    Enqueue

    Tutu's Gambit
}

Numerical Reflection: 200
{@str(Loop)} >>
Enqueue
